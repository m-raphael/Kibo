---
name: git-worktree
description: Use separate worktrees for parallel Claude sessions and isolated branches.
---

## Use when
Use when working on multiple tasks or branches in parallel.

## Output
- worktree path
- branch name
- cleanup steps
